import { ApplicationFileTransfer } from './application-file-transfer';

describe('ApplicationFileTransfer', () => {
  it('should create an instance', () => {
    expect(new ApplicationFileTransfer()).toBeTruthy();
  });
});
